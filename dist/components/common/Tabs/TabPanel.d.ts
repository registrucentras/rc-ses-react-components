import { default as React } from 'react';
type Props = {
    activeTab: number;
    children?: React.ReactNode;
    index: number;
};
declare function RcSesTabPanel({ activeTab, children, index, ...rest }: Props): import("react/jsx-runtime").JSX.Element;
export default RcSesTabPanel;
