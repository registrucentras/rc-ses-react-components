import type { SxProps, Theme } from '@mui/material'

type IconProps = {
  'aria-hidden'?: boolean
  className?: string
  fillColor?: string
  size?: number
  strokeColor?: string
  sx?: SxProps<Theme>
}

export default IconProps
